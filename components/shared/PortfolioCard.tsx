type PortfolioCardProps = {
    title: string;
    author: string;
    trade: string;
    description: string;
};

export default function PortfolioCard({
    title,
    author,
    trade,
    description,
}: PortfolioCardProps) {
    return (
        <>
            {/* Paste the entire portfolio card here */}
        </>
    );
}